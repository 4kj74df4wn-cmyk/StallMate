{
  "//": "StallMate P0 — STAGING deploy config (PREPARED, NOT USED). Repo: security/p0/staging/firebase.staging.json. Targets the staging project ONLY via --project staging alias. Deploy is gated on Blaze + Room 00 per-release authorization. Production project stallmate-9caac is never referenced here.",
  "database": { "rules": "database.rules.json" },
  "functions": { "source": "../functions", "runtime": "nodejs20" },
  "emulators": {
    "auth": { "host": "127.0.0.1", "port": 9099 },
    "database": { "host": "127.0.0.1", "port": 9000 },
    "functions": { "host": "127.0.0.1", "port": 5001 },
    "ui": { "enabled": false }
  }
}
