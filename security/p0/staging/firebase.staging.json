{
  "//": "StallMate P0 — STAGING deploy config (PREPARED, NOT USED). Repo: security/p0/staging/firebase.staging.json. Use ONLY with --project staging and --config security/p0/staging/firebase.staging.json. The production project is never referenced here (bindings live only in .firebaserc). SR3 deploys database.rules.B.json (Rules B); rollback deploys database.rules.forwardfix.json (secure last-known-secure), never the insecure legacy ruleset.",
  "database": { "rules": "database.rules.B.json" },
  "functions": { "source": "../functions", "runtime": "nodejs20" },
  "emulators": {
    "auth": { "host": "127.0.0.1", "port": 9099 },
    "database": { "host": "127.0.0.1", "port": 9000 },
    "functions": { "host": "127.0.0.1", "port": 5001 },
    "ui": { "enabled": false }
  }
}
