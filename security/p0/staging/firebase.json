{
  "//": "StallMate P0 §9.2 — staging/emulator tooling config ONLY (lives in security/p0/staging/). Database rules point at the reviewed Rules A for staging emulator testing. Emulator work uses the demo-* default alias (offline; cannot reach production). No Hosting/Functions/Auth deploy config here — nothing is deployed in §9.2.",
  "database": { "rules": "database.rules.json" },
  "emulators": {
    "database": { "host": "127.0.0.1", "port": 9000 },
    "ui": { "enabled": false },
    "singleProjectMode": false
  }
}
