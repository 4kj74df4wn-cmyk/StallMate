{
  "//": "StallMate P0 Cloud Functions manifest (DEPLOYABLE, NOT DEPLOYED). Repo: security/p0/functions/package.json. Node 20 (2nd-gen). Deploy gated on Blaze + Room 00.",
  "name": "stallmate-p0-functions",
  "description": "P0 owner-binding callable (bindOwner). Not deployed until Blaze + Room 00 authorization.",
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
